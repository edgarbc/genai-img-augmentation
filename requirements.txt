# Core dependencies for demo and lightweight experiments
numpy>=1.21.0
pillow>=8.0.0
scikit-image>=0.19.0

# Optional: PyTorch (CPU-only for CI compatibility)
# Uncomment for local experiments or use: pip install torch --index-url https://download.pytorch.org/whl/cpu
# torch

# Optional: MONAI for full generative model experiments
# Install separately with: pip install monai
# monai
